{
  "name": "discit-types",
  "version": "1.0.0",
  "description": "Shared types package for the discit, discit-api, and discit-api-refresh projects.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pack:light": "npm run build && npm pack --dry-run",
    "pack:release": "npm run build && npm pack",
    "release": "npm run build && npm publish"
  },
  "author": "Chris Leveille <cdleveille@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/cdleveille/discit-types"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.4.5"
  }
}
